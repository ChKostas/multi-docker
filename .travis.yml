sudo: required
services:
  - docker

before_install:
  - docker build -t chriskostas/react-test -f ./client/Dockerfile.dev ./client 

script:
  - docker run chriskostas/react-test npm test -- --coverage